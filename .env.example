# Production origin, without a trailing slash. Leave unset until the domain is decided.
# Enables canonical URLs, hreflang alternates, absolute Open Graph image URLs and the sitemap.
# SITE_URL=https://www.example.me
